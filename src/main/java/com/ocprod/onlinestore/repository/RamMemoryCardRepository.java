package com.ocprod.onlinestore.repository;

import com.ocprod.onlinestore.entity.RamMemoryCard;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RamMemoryCardRepository extends JpaRepository<RamMemoryCard, Long> {
}
